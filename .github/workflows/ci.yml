name: ci

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  RUST_TOOLCHAIN: 1.91.1
  RISC0_RUST_TOOLCHAIN: 1.91.1
  RZUP_VERSION: 0.5.1
  RISC0_GROTH16_PROVER_IMAGE: risczero/risc0-groth16-prover:v2025-04-03.1

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: go.mod

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-${{ env.RUST_TOOLCHAIN }}
          workspaces: |
            risc0/receipt -> target
            solana -> target

      - name: Cache RISC0 toolchain
        uses: actions/cache@v4
        with:
          path: ~/.risc0
          key: ${{ runner.os }}-risc0-rzup-rust-${{ env.RISC0_RUST_TOOLCHAIN }}

      - name: Install rzup
        run: |
          if command -v rzup >/dev/null; then
            if rzup --version | grep -q "${{ env.RZUP_VERSION }}"; then
              echo "rzup already installed: $(rzup --version)"
              exit 0
            fi
            echo "rzup present but wrong version: $(rzup --version)"
          fi
          cargo install rzup --version ${{ env.RZUP_VERSION }} --locked --force

      - name: Install RISC0 rust toolchain
        run: rzup install rust ${{ env.RISC0_RUST_TOOLCHAIN }}

      - name: Run tests
        run: make test

  groth16_gate:
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ steps.check.outputs.enabled }}
    steps:
      - name: Check Bonsai secrets
        id: check
        env:
          BONSAI_API_URL: ${{ secrets.BONSAI_API_URL }}
          BONSAI_API_KEY: ${{ secrets.BONSAI_API_KEY }}
        run: |
          if [[ -n "${BONSAI_API_URL}" && -n "${BONSAI_API_KEY}" ]]; then
            echo "enabled=true" >> "${GITHUB_OUTPUT}"
            exit 0
          fi
          echo "enabled=false" >> "${GITHUB_OUTPUT}"

  groth16:
    needs: groth16_gate
    if: ${{ needs.groth16_gate.outputs.enabled == 'true' }}
    runs-on: runs-on=${{ github.run_id }}/family=m7i.16xlarge/image=ubuntu22-full-x64/spot=pco/extras=s3-cache
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: RunsOn cache
        uses: runs-on/action@v2
        with:
          sccache: s3

      - name: Install sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: sccache version
        run: |
          which sccache
          sccache --version

      - name: Runner specs
        run: |
          echo "nproc: $(nproc || true)"
          lscpu || true
          free -h || true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-${{ env.RUST_TOOLCHAIN }}
          workspaces: |
            risc0/receipt -> target
            solana -> target

      - name: Cache RISC0 toolchain
        uses: actions/cache@v4
        with:
          path: ~/.risc0
          key: ${{ runner.os }}-risc0-rzup-rust-${{ env.RISC0_RUST_TOOLCHAIN }}

      - name: Install rzup
        run: |
          if command -v rzup >/dev/null; then
            if rzup --version | grep -q "${{ env.RZUP_VERSION }}"; then
              echo "rzup already installed: $(rzup --version)"
              exit 0
            fi
            echo "rzup present but wrong version: $(rzup --version)"
          fi
          cargo install rzup --version ${{ env.RZUP_VERSION }} --locked --force

      - name: Install RISC0 rust toolchain
        run: rzup install rust ${{ env.RISC0_RUST_TOOLCHAIN }}

      - name: Build Groth16 proof test (release)
        timeout-minutes: 60
        run: |
          set -euo pipefail
          (while sleep 60; do echo "[keepalive] $(date -u) still compiling..."; done) &
          keepalive_pid=$!
          trap 'kill ${keepalive_pid} >/dev/null 2>&1 || true' EXIT
          time cargo test --release --locked --manifest-path risc0/receipt/host/Cargo.toml --features bonsai --test groth16_bundle --no-run
          kill "${keepalive_pid}" >/dev/null 2>&1 || true
          trap - EXIT

      - name: Run Groth16 proof test (release, ignored)
        timeout-minutes: 60
        env:
          BONSAI_API_URL: ${{ secrets.BONSAI_API_URL }}
          BONSAI_API_KEY: ${{ secrets.BONSAI_API_KEY }}
          RISC0_PROVER: bonsai
          RUST_LOG: info
        run: |
          set -euo pipefail
          (while sleep 60; do echo "[keepalive] $(date -u) still proving..."; done) &
          keepalive_pid=$!
          trap 'kill ${keepalive_pid} >/dev/null 2>&1 || true' EXIT
          time cargo test --release --locked --manifest-path risc0/receipt/host/Cargo.toml --features bonsai --test groth16_bundle -- --ignored --nocapture
          kill "${keepalive_pid}" >/dev/null 2>&1 || true
          trap - EXIT

      - name: sccache stats
        if: always()
        run: sccache --show-stats || true
